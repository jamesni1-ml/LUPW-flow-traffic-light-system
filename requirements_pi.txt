# ── Raspberry Pi 4 — Common Dependencies ────────────────────────
# Required for both GPIO traffic light and monitor display modes
ultralytics>=8.0.0
opencv-python>=4.8.0
numpy>=1.24.0
picamera2>=0.3.12

# ── GPIO Traffic Light Mode (inference.py) ───────────────────────
# Only needed if using physical LEDs with transistor driver circuits
RPi.GPIO>=0.7.0

# ── Monitor Display Mode (monitor_display.py) ───────────────────
# No additional dependencies — uses opencv-python (above) for display
# Just connect a monitor via HDMI
